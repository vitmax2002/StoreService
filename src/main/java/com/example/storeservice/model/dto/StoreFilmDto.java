package com.example.storeservice.model.dto;

import com.example.storeservice.model.Film;

public record StoreFilmDto(int customer,
                           int film
                           ) {
}
